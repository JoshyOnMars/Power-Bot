const { MessageEmbed } = require('discord.js');

module.exports = {
	name: 'add',
	usage: "add",
  	category: 'Economy',
	async execute(message, args, client) {
        const member = 
        
	},
};
